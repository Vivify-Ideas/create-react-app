import { defineMessages } from 'react-intl';

export const scope = 'ResetPasswordPage';

export default defineMessages({
  resetPasswordTitle: {
    id: `${scope}.text.resetPasswordTitle`,
    defaultMessage: 'Reset Password'
  },
  resetPasswordButton: {
    id: `${scope}.button.resetPassword`,
    defaultMessage: 'Reset password'
  }
});
